<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.Fullscreen">
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
        <item name="android:fitsSystemWindows">@bool/fullscreen_style_fit_system_windows</item>
        <item name="android:windowTranslucentNavigation">@bool/fullscreen_style_use_translucent_nav</item>
    </style>

    <style name="AppTheme.AppBarOverlay"
           parent="ThemeOverlay.AppCompat.Dark.ActionBar"/>

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Dark"/>

    <style name="FastScrollBubbleText" parent="TextAppearance.AppCompat.Headline">
        <item name="android:textColor">@color/black</item>
        <item name="android:textSize" tools:ignore="SpUsage">26dp</item>
    </style>

    <style name="AppTheme.RuleTextContent" parent="TextAppearance.AppCompat.Body1">
        <item name="android:textColorLink">@color/link</item>
    </style>

</resources>
